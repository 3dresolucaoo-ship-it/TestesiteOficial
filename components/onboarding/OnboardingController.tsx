'use client'

/**
 * OnboardingController — orquestrador do wizard de onboarding.
 *
 * TODO Onda 5 (pre-launch 11/06): implementar wizard completo.
 * Copy pronta em brand/onboarding-copy-2026-05-20.md (Carla).
 * Referencia: Felipe implementa o wizard, este componente orquestra
 * quando exibir (first login, project vazio, etc.).
 *
 * Stub criado pra desbloquear build (modulo importado em AppShell.tsx
 * mas nao existia). Sem comportamento ate implementacao real.
 */

export function OnboardingController() {
  // TODO: implementar onboarding wizard (Felipe · Onda 5 pre-launch)
  return null
}
